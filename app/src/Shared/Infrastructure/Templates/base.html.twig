<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="{{ app_name }} - профессиональная платформа для управления документами и покрытиями">
    <title>{% block title %}{{ app_name }} | {% endblock %}</title>

    {# Favicon и манифест #}
    <link rel="apple-touch-icon" sizes="180x180" href="{{ asset('icons/apple-touch-icon.png') }}">
    <link rel="icon" type="image/png" sizes="32x32" href="{{ asset('icons/favicon-32x32.png') }}">
    <link rel="icon" type="image/png" sizes="16x16" href="{{ asset('icons/favicon-16x16.png') }}">
    <link rel="icon" href="{{ asset('icons/favicon.ico') }}" sizes="16x16 32x32"/>
    <link rel="manifest" href="{{ asset('icons/site.webmanifest') }}">

    {# Bootstrap Icons #}
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">

    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}

    <style>
        :root {
            --primary-color: #007bff;
            --secondary-color: #6c757d;
        }

        body {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        main {
            flex: 1;
        }

        .blog-header {
            border-bottom: 1px solid #e5e5e5;
            position: relative;
            z-index: 1030;
            background: white;
        }

        .blog-header-logo {
            font-family: "Playfair Display", Georgia, "Times New Roman", serif;
            font-size: 2.25rem;
            text-decoration: none;
        }

        .blog-header-logo:hover {
            text-decoration: none;
        }

        .flash-messages {
            position: fixed;
            top: 90px;
            right: 20px;
            z-index: 1050;
            max-width: 400px;
        }

        /* Мобильное меню - бургер */
        .mobile-menu-toggle {
            position: fixed;
            top: 15px;
            left: 15px;
            z-index: 1040;
            width: 40px;
            height: 40px;
            padding: 0;
            border-radius: 6px;
            display: none;
        }

        .mobile-menu-toggle .bi-list {
            font-size: 1.2rem;
        }

        /* Адаптивность */
        @media (max-width: 768px) {
            .mobile-menu-toggle {
                display: flex;
                align-items: center;
                justify-content: center;
            }

            .flash-messages {
                top: 100px;
                left: 20px;
                right: 20px;
                max-width: none;
            }

            .blog-header-logo {
                font-size: 1.75rem;
            }

            .blog-header .flex-nowrap {
                flex-wrap: wrap !important;
            }

            .blog-header .col-4 {
                flex: 0 0 100%;
                max-width: 100%;
                text-align: center !important;
            }

            /* Скрываем элементы на мобильных */
            .blog-header .link-secondary {
                display: none;
            }

            .blog-header .dropdown {
                display: none !important;
            }

            .blog-header .btn-outline-secondary {
                display: none !important;
            }

            /* Уменьшаем отступы */
            .blog-header {
                padding-top: 0.5rem;
                padding-bottom: 0.5rem;
            }
        }

        @media (max-width: 576px) {
            .blog-header-logo {
                font-size: 1.5rem;
            }

            .mobile-menu-toggle {
                top: 12px;
                left: 12px;
                width: 36px;
                height: 36px;
            }

            .flash-messages {
                top: 90px;
            }
        }

        .back-to-top {
            position: fixed;
            bottom: 20px;
            right: 20px;
            z-index: 1000;
            display: none;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .user-avatar {
            width: 32px;
            height: 32px;
            border-radius: 50%;
            object-fit: cover;
            background-color: #dee2e6;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .user-avatar i {
            font-size: 16px;
            color: #6c757d;
        }

        /* Offcanvas меню */
        .offcanvas-header {
            border-bottom: 1px solid #dee2e6;
            padding: 1rem 1.5rem;
        }

        .offcanvas-title {
            font-weight: 600;
        }

        .offcanvas-body {
            padding: 0;
        }

        .list-group-item {
            border: none;
            border-radius: 0;
            padding: 1rem 1.5rem;
            border-bottom: 1px solid #f8f9fa;
        }

        .list-group-item:hover {
            background-color: #f8f9fa;
        }

        .list-group-item i {
            width: 20px;
        }

        /* Секция пользователя в мобильном меню */
        .user-section {
            background-color: #f8f9fa;
            border-bottom: 1px solid #dee2e6;
        }

        .user-info {
            padding: 1rem 1.5rem;
        }

        .user-email {
            font-weight: 500;
            color: #495057;
        }
    </style>
</head>
<body>
{# Кнопка мобильного меню - ВНЕ какого-либо блока #}
<button class="btn btn-primary mobile-menu-toggle shadow-sm" type="button"
        data-bs-toggle="offcanvas" data-bs-target="#mobileMenu">
    <i class="bi bi-list"></i>
</button>

{% block header %}
    <header class="blog-header py-3">
        <div class="container-fluid">
            <div class="row flex-nowrap justify-content-between align-items-center">
                {# Левая колонка - пустая для баланса #}
                <div class="col-4 pt-1 d-none d-md-block"></div>

                {# Центральная колонка - логотип #}
                <div class="col-4 text-center">
                    <a class="blog-header-logo text-dark" href="{{ path('app_homepage') }}">
                        {{ app_name }}
                    </a>
                </div>

                {# Правая колонка - поиск и пользователь (только для десктопа) #}
                <div class="col-4 d-flex justify-content-end align-items-center d-none d-md-flex">
                    {# Иконка поиска #}
                    <a class="link-secondary me-3" href="{{ path('app_cabinet_document_list') }}" aria-label="Поиск"
                       data-bs-toggle="tooltip" data-bs-placement="bottom" title="Поиск документов">
                        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" stroke="currentColor"
                             stroke-linecap="round" stroke-linejoin="round" stroke-width="2" class="mx-3" role="img"
                             viewBox="0 0 24 24">
                            <circle cx="10.5" cy="10.5" r="7.5"></circle>
                            <path d="M21 21l-5.2-5.2"></path>
                        </svg>
                    </a>

                    {# Блок пользователя - ТОЛЬКО ДЛЯ ДЕСКТОПА #}
                    {% if app.user %}
                        <div class="dropdown">
                            <a href="#" class="d-block link-dark text-decoration-none dropdown-toggle d-flex align-items-center"
                               id="dropdownUser1" data-bs-toggle="dropdown" aria-expanded="false">
                                <div class="user-avatar me-2">
                                    <i class="bi bi-person"></i>
                                </div>
                                <span class="d-none d-md-inline">{{ app.user.email }}</span>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end text-small" aria-labelledby="dropdownUser1">
                                <li><a class="dropdown-item" href="{{ path('app_cabinet') }}">
                                        <i class="bi bi-person-circle me-2"></i>Кабинет
                                    </a></li>
                                <li><a class="dropdown-item" href="{{ path('app_cabinet_proposals_general_proposal_list') }}">
                                        <i class="bi bi-files me-2"></i>Мои формы
                                    </a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="{{ path('app_logout') }}">
                                        <i class="bi bi-box-arrow-right me-2"></i>Выйти
                                    </a></li>
                            </ul>
                        </div>
                    {% else %}
                        <a class="btn btn-sm btn-outline-secondary" href="{{ path('app_login') }}">
                            Войти
                        </a>
                    {% endif %}
                </div>
            </div>
        </div>
    </header>
{% endblock %}

{# Flash сообщения #}
<div class="flash-messages">
    {% for type, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert alert-{{ type }} alert-dismissible fade show shadow" role="alert">
                {{ message }}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        {% endfor %}
    {% endfor %}

    {% if is_granted('IS_IMPERSONATOR') %}
        <div class="alert alert-warning alert-dismissible fade show shadow" role="alert">
            Вы находитесь в режиме исполнения роли
            <a href="{{ impersonation_exit_path(path('app_homepage')) }}" class="alert-link">Выйти из режима</a>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    {% endif %}
</div>

<main class="container-fluid">
    <div class="container py-4">
        {% block body %}{% endblock %}
    </div>
</main>

{# Кнопка "Наверх" #}
<button class="btn btn-primary back-to-top shadow" onclick="scrollToTop()" title="Наверх">
    <i class="bi bi-arrow-up"></i>
</button>

{% block footer %}
    <footer class="bg-dark text-white py-4 mt-auto">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-md-6 mb-3 mb-md-0">
                    <p class="mb-0">&copy; {{ "now"|date("Y") }} 1helper</p>
                </div>
                <div class="col-md-6 text-md-end">
                    <ul class="nav justify-content-md-end">
                        <li class="nav-item">
                            <a href="#" class="nav-link px-2 text-body-secondary text-white-50" onclick="scrollToTop(); return false;">Наверх</a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </footer>
{% endblock %}

{# Мобильное offcanvas меню - ТОЛЬКО ОДНО #}
<div class="offcanvas offcanvas-start" tabindex="-1" id="mobileMenu" aria-labelledby="mobileMenuLabel">
    <div class="offcanvas-header">
        <h5 class="offcanvas-title" id="mobileMenuLabel">Меню</h5>
        <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Закрыть"></button>
    </div>
    <div class="offcanvas-body">
        {# Секция информации о пользователе #}
        {% if app.user %}
            <div class="user-section">
                <div class="user-info">
                    <div class="d-flex align-items-center">
                        <div class="user-avatar me-3">
                            <i class="bi bi-person"></i>
                        </div>
                        <div>
                            <div class="user-email">{{ app.user.email }}</div>
                            <small class="text-muted">Авторизован</small>
                        </div>
                    </div>
                </div>
            </div>
        {% endif %}

        <div class="list-group list-group-flush">
            <a href="{{ path('app_homepage') }}" class="list-group-item list-group-item-action">
                <i class="bi bi-house me-2"></i>Главная
            </a>
            <a href="{{ path('app_cabinet_document_list') }}" class="list-group-item list-group-item-action">
                <i class="bi bi-search me-2"></i>Поиск документов
            </a>
            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('app_cabinet_coating_coating_list') }}" class="list-group-item list-group-item-action">
                    <i class="bi bi-grid me-2"></i>Покрытия
                </a>
                <a href="{{ path('app_cabinet_coating_manufacturer_list') }}" class="list-group-item list-group-item-action">
                    <i class="bi bi-building me-2"></i>Производители
                </a>
                <a href="{{ path('app_cabinet_document_create') }}" class="list-group-item list-group-item-action">
                    <i class="bi bi-file-text me-2"></i>Документы
                </a>
            {% endif %}
            <a href="{{ path('app_cabinet_proposals_general_proposal_list') }}" class="list-group-item list-group-item-action">
                <i class="bi bi-files me-2"></i>Мои формы
            </a>
            <hr class="my-2">

            {# Ссылки связанные с аккаунтом #}
            {% if app.user %}
                <a href="{{ path('app_cabinet') }}" class="list-group-item list-group-item-action">
                    <i class="bi bi-person-circle me-2"></i>Личный кабинет
                </a>
                <a href="{{ path('app_logout') }}" class="list-group-item list-group-item-action text-danger">
                    <i class="bi bi-box-arrow-right me-2"></i>Выйти из аккаунта
                </a>
            {% else %}
                <a href="{{ path('app_login') }}" class="list-group-item list-group-item-action">
                    <i class="bi bi-box-arrow-in-right me-2"></i>Войти в аккаунт
                </a>
                <a href="{{ path('app_sign_up') }}" class="list-group-item list-group-item-action">
                    <i class="bi bi-person-plus me-2"></i>Зарегистрироваться
                </a>
            {% endif %}
        </div>
    </div>
</div>

<script>
    // Скролл к верху страницы
    function scrollToTop() {
        window.scrollTo({ top: 0, behavior: 'smooth' });
    }

    // Показ/скрытие кнопки "Наверх"
    window.addEventListener('scroll', function() {
        const backToTopBtn = document.querySelector('.back-to-top');
        if (window.scrollY > 300) {
            backToTopBtn.style.display = 'flex';
        } else {
            backToTopBtn.style.display = 'none';
        }
    });

    document.addEventListener('DOMContentLoaded', function() {
        // Автоматическое скрытие alert'ов через 5 секунд
        const alerts = document.querySelectorAll('.alert');
        alerts.forEach(alert => {
            setTimeout(() => {
                const bsAlert = new bootstrap.Alert(alert);
                bsAlert.close();
            }, 5000);
        });

        // Инициализация tooltips
        const tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'));
        tooltipTriggerList.map(function (tooltipTriggerEl) {
            return new bootstrap.Tooltip(tooltipTriggerEl);
        });

        // Удаляем все дубликаты бургер-меню, оставляем только первый
        const mobileToggles = document.querySelectorAll('.mobile-menu-toggle');
        if (mobileToggles.length > 1) {
            for (let i = 1; i < mobileToggles.length; i++) {
                mobileToggles[i].remove();
            }
        }

        // Удаляем все дубликаты offcanvas меню, оставляем только первый
        const offcanvasMenus = document.querySelectorAll('#mobileMenu');
        if (offcanvasMenus.length > 1) {
            for (let i = 1; i < offcanvasMenus.length; i++) {
                offcanvasMenus[i].remove();
            }
        }

        // Закрытие мобильного меню при клике на ссылку
        document.querySelectorAll('#mobileMenu .list-group-item').forEach(item => {
            item.addEventListener('click', function() {
                const bsOffcanvas = bootstrap.Offcanvas.getInstance(document.getElementById('mobileMenu'));
                if (bsOffcanvas) {
                    bsOffcanvas.hide();
                }
            });
        });
    });
</script>
</body>
</html>